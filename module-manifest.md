# Compositor Windows module map

Public open-source Windows 10 image editor derived from Robbie Tilton's MIT-licensed Compositor. Upstream Swift/AppKit/Metal sources remain intact; the Windows implementation replaces those platform facilities with Qt, Pillow, NumPy and OpenCV. This is a native Windows reimplementation within the fork, not SwiftUI running on Windows.

The public app runs independently of the Axiomatic workspace. General editing, generation and MCP are core capabilities. Owen & Vic is an optional local-service connector. Per-user state lives under `%LOCALAPPDATA%/Compositor`, overridable through `COMPOSITOR_DATA` for isolated verification.

| Owner | Source | Contract |
| --- | --- | --- |
| Document and history | `windows/compositor/document.py` | Single transactional document model; immutable pixel references, editable raster/text/shape/gradient/adjustment/group layers, masks, selections, transforms, paint/retouch, file formats and bounded in-memory undo. History state identity determines unsaved changes; monotonic revisions independently reject stale operations. |
| Rendering | `windows/compositor/pixels.py` | sRGB RGBA compositing, 20 blend modes, shapes/text/gradients, layer effects, filters and adjustments. Mirrors upstream transform and blend intent with Windows libraries. |
| Pixel art | `windows/compositor/pixel_art.py`, `windows/compositor/pixel_ui.py` | Independent low-resolution conversion documents, previewed resolution/palette/reduction/dither/simplification/alpha threshold. Pixel metadata and per-layer nearest sampling persist in native files. Pencil and pixel eraser, crisp zoom/grid, clickable palette, lossless MCP inspection and integer nearest export support subsequent cleanup. Automatic conversion is not finished pixel-cluster authorship. |
| Generation source geometry | `windows/compositor/generation_source.py` | API and subscription share contextual crops, 64-aligned working dimensions, explicit smooth/nearest sampling, aspect-preserving edge padding and inverse placement. Selection masks bound application independently of context. Provider bytes, actual dimensions and normalized candidate dimensions remain distinct. |
| Shared workspace | `windows/compositor/workspace.py` | Qt owner thread; both native UI and HTTP/MCP enter the same dispatch. Open documents, revisions, recovery, generation application, inspection and optional connector. |
| Native editor | `windows/compositor/ui.py`, `windows/compositor/chrome.py` | Qt canvas with pixel rulers, original vector tool icons, direct color selection, stacked Color/Properties/Layers inspector and optional generation, ChatGPT and project panels. Window geometry and named dock/toolbar state persist in per-user `window.json`. Human edits use the canonical workspace commands. |
| Typography | `windows/compositor/fonts.py`, `windows/compositor/text_ui.py` | Installed Windows font families and faces, immediate family/style/size controls in Properties, searchable selection and an expanded text dialog with actual rendered preview. Unfinished property input stays pinned to its original document/layer; field conflicts preserve the draft for explicit retry, while unrelated edits are merged. MCP discovers the same fonts. |
| Gesture previews | `windows/compositor/gesture.py` | Immutable brush/eraser snapshots rendered through the same pixel operations as committed edits. Pointwise documents render bounded patches; effects and adjustments use the full renderer. Preview pixels replace a temporary canvas bitmap, preserving transparency and fractional-zoom edges. Escape cancels; release commits one undo step; external document changes cancel the pending gesture. |
| Public production projects | `windows/compositor/projects.py`, `windows/compositor/production_ui.py` | Standalone artwork/comic/book projects, lazily loaded canonical page documents, authored prose, project direction, labeled references, portable `.compbook` files, HTML/PDF reading exports and shared human/MCP presentation. Page drafts persist separately until saved. |
| Reading exports | `windows/compositor/reading.py` | Self-contained responsive HTML and PDF pages at the artwork's aspect ratio. Additional prose is typeset below the image at readable size; composed comic pages acquire no report headings or A4 margins. |
| Creative direction | `windows/compositor/creative.py`, `windows/compositor/creative_ui.py`, `windows/compositor/prompts/styles.json` | Built-in and custom reusable styles with prefix/suffix and image references; project-owned snapshots, named characters, page cast, retained reference-sheet candidates and one inspectable prompt composer for API/MCP/subscription preparation. |
| Generation | `windows/compositor/generation.py`, `windows/compositor/candidate_ui.py` | Configurable OpenAI-compatible and Gemini providers, immutable input capture, asynchronous jobs, actual returned dimensions, retained candidates, exact-source application and explicit recovery after interruption. Candidate review and application share the same layer preparation. Pixel documents use the logical grid, nearest sampling, hard alpha and default document palette, with candidate colors available in review. No provider output-token caps. |
| Subscription collaboration | `windows/compositor/chat.py`, `windows/compositor/prompts/editor.md` | Official Codex App Server stdio; isolated managed ChatGPT OAuth, email/action state, GPT-6-Sol default and persisted model choice, app-managed or selected working folder, automatic Windows sandbox setup, approved owned MCP connection, panel-based questions, persistent conversation and native image-result ingestion. UI and authenticated HTTP drive the same session without desktop automation. |
| Optional runtime installation | `windows/compositor/codex_runtime.py` | Downloads the pinned official Codex Windows package when no runtime is installed; verifies registry SHA-512 integrity and extracts into per-user app storage. |
| Configuration | `windows/compositor/settings.py` | Public per-user settings; API keys in Windows Credential Manager or environment; local operator capability in a separate file. No household paths or keys in defaults. |
| Agent transport | `windows/compositor/server.py`, `windows/compositor/mcp_server.py` | Authenticated loopback command queue plus portable stdio MCP. Inspection returns images from actual document rendering. HTTP work runs on the same Qt owner and rejects stale revisions. |
| Production connector | `windows/compositor/connectors.py` | Optional Owen & Vic HTTP adapter: project listing, selected image pull, copy-back and existing authoring/generation/presentation/export operations. Never opens its canonical data in a second writer. |
| Installation | `windows/run.py`, `windows/setup.ps1`, `windows/build.ps1`, `windows/Compositor.spec`, `windows/install.ps1`, `windows/start-desktop.ps1` | Isolated dependencies, standalone GUI and console MCP binaries, per-user installation/shortcuts and independent interactive launch. Single-instance lock precedes recovery and job loading. |
| Verification | `windows/tests/` | Pixel contracts, masks, selection, blend alpha, persistence, undo, stale candidates, real Qt pointer input and HTTP-to-UI shared ownership. Tests use temporary stores and fake providers; they spend no image credits. |

## State and preservation

`.compwin` is a ZIP with versioned JSON, lossless source layers/masks, selection and a merged preview. Atomic replacement occurs only after full serialization. `.ora` adds standard OpenRaster layers for compatible documents and retains the native manifest for exact Compositor reopening; unsupported portable semantics produce an error. Imported raster originals remain untouched. Export refuses their original paths.

Opening a native document already owned under the same file path activates that working document and returns its actual state. A different file with a colliding embedded ID receives an independent in-memory identity. `asCopy` explicitly creates an unsaved independent document. This prevents Save-as variants from silently selecting an original tab while reporting the unopened variant's contents.

Undo/redo carries an artwork-state identity that persists in native saves and recovery; numeric revisions independently advance during the current editing session to reject stale concurrent edit commands. Returning to the last saved state clears the dirty marker. Generation candidates bind to document ID and exact artwork-state identity, so an unchanged reopen or undo back to the captured state permits placement. A different state cannot become current merely because reopening resets the numeric revision. Old native files derive their initial identity from manifest content and decoded layer/mask/selection pixels, excluding the selected editing layer and ZIP metadata. Their original files remain untouched until explicitly saved.

Recovery saves open documents and a session index under the user's app data. It is separate from an explicit document save. Generation folders retain requests, captured source/reference images, original provider bytes, normalized candidates and application provenance. The native subscription runtime stores its own authentication and conversations under the app's isolated `chat/codex` directory. Compositor never borrows the developer's Codex authentication.

## Current implementation frontier

Judge progress through actual creative work in the installed app and inspection of saved and exported artifacts. Do not add tests as a routine implementation step or substitute suite totals for designer, artist and comic-creator use. Existing checks cover bounded machine contracts; they do not establish that a workflow is usable or its result is professional.

The installed GUI and packaged MCP have been exercised together: document creation, editing, stale-revision rejection, actual image inspection, layered save, export and undo. A three-page comic has now been composed through the live owner API with editable Segoe UI captions, a retained native subscription image, named character reference and project style. Saving and reopening retained those layers and creative assets. Reading the actual PDF exposed nearly invisible page prose and inappropriate report layout; `reading.py` replaces that export path with readable prose and page-sized artwork.

The installed GPT-6-Sol collaborator also completed an actual caption-fit request: it inspected the page, narrowed only the bottom cream box from 936 to 646 pixels and inspected the result. The foreground window stayed unchanged during that turn. After installing the export correction, the same comic was saved, exported and reopened through the installed owner. The resulting PDF has three square pages; its final page and the browser-rendered HTML were visually inspected. Original open work survived the update. This establishes this creative journey, not general Photoshop parity or perfect character consistency across generators.

In a subsequent actual design task, in-app GPT-6-Sol built a separate 1200-by-1600 poster from the retained raster, with editable Bodoni MT, Georgia and Gill Sans MT type. It inspected placement, saved the layered document and exported a PNG without changing foreground focus during the 181-second turn. A rejected two-pixel rule exposed incorrectly inset Pillow shape bounds; shapes now use their full pixel bounds, and the installed poster uses one- and two-pixel rules. The real comic then exercised remembered Save, normal Open returning to the same project, and explicit Open a copy for an independent serif-lettered edition. Text Properties now exposes family, style and size. Those controls were used in the source GUI to create a Bodoni variant; the installed app subsequently opened, saved and exported it alongside the original, with the live Properties panel visually inspected. The duplicate embedded document identity discovered during that journey now receives an independent owner for a different file.

Editor chrome is verified separately from document operations. The GUI tests exercise color selection, visibility eye controls, pixel-dimension transforms and keeping Layers visible while switching secondary panels. Rendered checks cover 1600×1000 and 1280×800 windows. The inspector retains a bounded width when a secondary panel closes. Visual review remains necessary; these checks do not establish professional finish by themselves.

Brush and eraser previews match committed pixels across masks, selections, transforms, grouping, blend modes, effects and adjustments. Tests inspect actual viewport pixels before release, cancellation, external edits during a stroke, fractional zoom and one-step undo. Synthetic tablet events verify pressure delivery and tapering; physical tablet hardware remains unverified. A 315×304 stroke preview measured about 4–5 ms on 1024–4096 square canvases in the bounded path, excluding viewport drawing. Complex effects use a slower full-render path. Workspace view state identifies a pending gesture while document inspection continues to return committed pixels.

Production tests verify editable page layers, prose and reference roles across a portable save/reopen, independent identities for opened copies, retained drafts through agent refreshes and restart, presentation navigation through the canonical page, and actual PDF rendering plus extractable prose. `.compbook` contains `project.json`, page `.compwin` archives and copied reference images. Project style snapshots and named characters travel with the bundle. Page generation includes only the selected named cast, plus project style and general references. Reference sheets require explicit acceptance before becoming character references. Library changes leave existing project styles intact. Both API generation and subscription preparation retain the assembled prompt, creative context and ordered image inputs.

Local project state lives under per-user `projects/`; opened pages are owned by Workspace, and other pages load on demand.

`project-files.json` owns local project-to-bundle paths and SHA-256 fingerprints. Save remembers the chosen file; normal Open activates its existing project when that file is unchanged, including any current unsaved work. Explicit `asCopy` imports fresh identities without a file binding. Externally changed files open independently and take the file binding, preserving the old working project. Paths never enter portable bundles. The Projects panel exposes Save as/Open a copy and distinguishes duplicate titles with file names or working-copy labels.

Live OpenAI generation returned a 1024×1024 image. A separate crop edit sent a 512×512 source and retained the 1024×1024 result; application preserved every pixel outside the crop. An optional Studio push/pull round trip through a dedicated test project preserved exact pixels. Codex App Server 0.156.0 was downloaded with SHA-512 verification. Live managed OAuth, email display and GPT-6-Sol conversation are proven. The signed-in model used the owned MCP server to inspect the project/cast, add and hide a layer, then undo both edits; original layers were restored. Foreground window identity stayed unchanged throughout the 41-second turn. A native subscription generation then returned a 1254-by-1254 square candidate with the project style and Mina character reference. Its input copy, prompt, creative context and source revision were retained; the original page stayed unchanged. The initial image attempt exposed missing Windows sandbox initialization; setup now runs automatically without elevation before account discovery. Foreground sampling during the image turn recorded one three-second visit to Compositor and return; its cause is not established by the samples.

Generate, project pages and character sheets share an explicit API/subscription route. Subscription requests retain the composed prompt and immutable source/reference inputs before the conversation starts. Native image results bind to that prepared request even if another document becomes active. External clients share the same stdio tools, including pixel-art conversion; the in-app connection approves only the owned Compositor server automatically. Background launches use `--background`, and chat questions live in the panel rather than modal windows.

Chat requests queue through initial setup and account discovery, retaining the intended document and generation inputs. A sent-message event owns transcript display and clearing the matching input draft; newer typed drafts survive. Stop cancels queued work. First Send installs missing support automatically. Working-folder changes validate the destination and reconnect an idle session; active requests must be stopped first. Replies render Markdown, and a local timer displays elapsed time and the age of the last runtime message. Lifecycle tests cover setup, draft retention, cancellation, document identity, folder changes, Markdown and restored layout. Commit `0cf485b` passed clean Windows CI run `35820680597`.

Layer clicks and MCP selection now share the same operation without creating undo/dirty state. Recovery records the active layer separately from artwork revision. Actual native-candidate application exposed a separate dirty-state omission; generation application now advances the saved-state identity and retains the selection as an editable mask.

Frank made true pixel-art creation and conversion the immediate creative priority. He rejected the first 128×128 area-averaged, 24-color Mina conversion as blurry. Actual canvas pixels matched the nearest-neighbor enlargement exactly; the softness was in the conversion, not display interpolation. The installed editor then created independent 128- and 256-pixel nearest-neighbor studies. The 256-pixel, 32-color version preserves facial and costume contours more clearly; source defaults now use nearest-neighbor reduction and a visible 256-pixel starting width. Maximum-coverage palette selection with refinement preserves highlights that the initial median-cut palette lost. A separate sky-accent layer exercises one-pixel drawing with an existing palette color. Converted work still needs deliberate contour and cluster cleanup; the in-app face-cleanup request was stopped when Frank challenged the source quality, before it changed the artwork.

Regional generation previously used a 260×150 source crop and returned 1651×953; the contextual, 64-aligned pipeline replaces that approach. A native subscription redraw has now used the 256-pixel Mina study through a 1024×1024 contextual source. The provider returned 1254×1254; retained provider bytes and the normalized 1024×1024 candidate are separate. The installed in-app agent applied it to the independent study and created further logical-grid conversions. The redraw simplified some forms but did not itself satisfy strict pixel-grid or palette authorship.

Frank confirmed that nearest-256 has crisp edges and is stage one of conversion. Facial readability is the next artistic problem. Buddy's separate 98-pixel pencil pass was rejected: joining the nose and glasses produced a pig-like nose and made the face worse. The original study is active again; that failed drawing must not become an automatic cleanup recipe. Palette membership and hard edges establish pixel mechanics, not artistic quality. `pixel_strokes` applies a coordinated set of hard-pencil strokes as one transaction and undo step; MCP exposes it directly. Candidate review now shows actual placement at logical resolution, with current-artwork comparison, integer zoom, document-palette/candidate-color choices and the saved selection mask. Preview and application share one preparation function. Further facial work needs to preserve the original anatomy and expression, with close and normal-size inspection before proposing an improvement.

API and subscription requests retain `sourceStateId`; queued chat setup preserves it with the intended document. Candidate review and application share one eligibility decision. Legacy candidates without a recorded source identity remain previewable and available for manual layer placement, but cannot automatically claim a match from a coincidentally equal revision number.

Installed use created a separate 128×128 satchel prop canvas and generated a subscription candidate with the existing moon-courier scene as reference. The 1254×1254 provider result was retained alongside a normalized 1024×1024 candidate. Compositor then restarted with all 18 documents intact, recovered the candidate's matching source identity and applied it as an editable 128×128 nearest-neighbor layer. The layered study and 4× PNG were saved while Mina's original conversion remained the visible document. The prop is a study, not a claim of finished pixel-art quality. Transient subscription reconnection notices now display their readable message instead of a raw error dictionary.

Remaining upstream feature gaps include automatic subject selection/background removal, perspective distortion, independently transformed linked/unlinked masks, GPU rendering, live clone/heal/blur feedback, richer inline typography, and several upstream import/filter details. Separable blending uses bounded strips to limit temporary memory. PSD preserves raster channels, hidden layers, opacity, groups and pixel masks; unsupported effects and modes produce conversion notes. PSD and upstream imports do not yet promise pixel-exact compatibility.

## Commands

- `powershell -File windows/setup.ps1`: install isolated Python dependencies.
- `.venv/Scripts/python.exe windows/run.py`: development GUI.
- `.venv/Scripts/python.exe windows/run.py --mcp`: stdio MCP connecting to the open app.
- `.venv/Scripts/python.exe -m pytest windows/tests -q`: focused verification.
- `powershell -File windows/build.ps1`: standalone Windows distribution in `dist/Compositor`.
- `powershell -File windows/install.ps1`: install a versioned local copy and shortcuts.
- `powershell -File windows/start-desktop.ps1`: independent Windows task launch for agent-operated installation.
